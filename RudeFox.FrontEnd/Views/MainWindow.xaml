<Window x:Class="RudeFox.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RudeFox.Views"
        mc:Ignorable="d"
        xmlns:vm="clr-namespace:RudeFox.ViewModels"
        xmlns:gong="urn:gong-wpf-dragdrop"
        Title="Rude Fox" Height="300" Width="300">
    <Window.DataContext>
        <vm:MainWindowVM />
    </Window.DataContext>

    <ListView gong:DragDrop.IsDropTarget="True" gong:DragDrop.DropHandler="{Binding}" ItemsSource="{Binding WorkItems}">
        <ListView.ItemTemplate>
            <DataTemplate DataType="vm:WorkItemVM">
                <StackPanel>
                    <TextBlock FontSize="16" FontWeight="Light" Text="{Binding Name}"/>
                    <TextBlock Text="{Binding Type}" />
                </StackPanel>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</Window>
