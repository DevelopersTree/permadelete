<Window x:Class="RudeFox.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RudeFox.Views"
        xmlns:frontEnd="clr-namespace:RudeFox.FrontEnd"
        mc:Ignorable="d"
        xmlns:converters="clr-namespace:RudeFox.Converters"
        xmlns:vm="clr-namespace:RudeFox.ViewModels" WindowState="Maximized" WindowStartupLocation="CenterScreen"
        xmlns:gong="urn:gong-wpf-dragdrop" Icon="/Images/icon_24.png"
        Title="{Binding Title}" Height="600" Width="800">
    <Window.Resources>
        <converters:TimespanToStringConverter x:Key="timeConverter"/>
    </Window.Resources>
    <Window.DataContext>
        <vm:MainWindowVM />
    </Window.DataContext>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="E_xit" Command="{Binding ExitCommand}"/>
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="_About" Command="{Binding ShowAboutCommand}"/>
            </MenuItem>
        </Menu>
        <ListBox gong:DragDrop.IsDropTarget="True" gong:DragDrop.DropHandler="{Binding}" ItemsSource="{Binding Source={x:Static frontEnd:App.Operations}}" HorizontalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="vm:WorkItemVM">
                    <Grid Margin="0, 5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition Height="8" />
                            <RowDefinition />
                        </Grid.RowDefinitions>

                        <Image Width="32" VerticalAlignment="Center" Grid.RowSpan="3" Source="{Binding Image}" Margin="10, 0" />

                        <DockPanel Grid.Column="1" LastChildFill="True">
                            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                <TextBlock Foreground="#555" Text="{Binding Size}" Margin="0,0, 10,0"  DockPanel.Dock="Right"/>
                                <TextBlock Foreground="#999" Text="—" Margin="0,0,5,0"/>
                                <TextBlock Foreground="#555" Text="{Binding Progress, StringFormat='{}{0:00.0}%', FallbackValue=0.0}" Margin="0,0, 10,0"  DockPanel.Dock="Right"/>
                                <TextBlock Foreground="#999" Text="—" Margin="0,0,5,0"/>
                                <TextBlock HorizontalAlignment="Right" Foreground="#555"  DockPanel.Dock="Right"
                                       Text="{Binding TimeRemaining, Converter={StaticResource timeConverter}}"/>
                            </StackPanel>

                            <TextBlock FontSize="14" FontWeight="Light" Text="{Binding Path}" DockPanel.Dock="Left" />
                        </DockPanel>

                        <ProgressBar Value="{Binding Progress}" Grid.Column="1" Grid.Row="2" DockPanel.Dock="Right" />

                        <Button Content="X" Grid.Column="2" Grid.Row="2" Margin="10, 0" Padding="10,0" VerticalAlignment="Center" Command="{Binding CancelCommand}"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.Style>
                <Style TargetType="{x:Type ListBox}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Path=Items.Count, RelativeSource={RelativeSource Self}}"  Value="0">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate>
                                        <Border BorderThickness="1" Background="Transparent" BorderBrush="DarkGray">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="*"/>
                                                    <RowDefinition Height="256"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="2*" />
                                                </Grid.RowDefinitions>
                                                <Image Height="128" Width="128" Grid.Row="1" Source="/Images/icon_128.png"/>
                                                <StackPanel Orientation="Horizontal" Grid.Row="2"  HorizontalAlignment="Center" Margin="0,0,0,20">
                                                    <TextBlock Text="Nothing to delete" FontSize="28" Margin="0,0,10,0" FontWeight="Light" />
                                                    <!--<Image Width="32" Height="32" Source="/Images/sad_32.png"/>-->
                                                </StackPanel>
                                                <TextBlock HorizontalAlignment="Center" Grid.Row="3" FontSize="18">Drop some files and folders here to be deleted.</TextBlock>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListBox.Style>
        </ListBox>
    </DockPanel>
</Window>
